# Local development for @partha/docs (port 4002)
#
#   cp .env.example .env.local
#
# Fumadocs does not use @t3-oss/env-nextjs today; this file is for consistency
# and future public URL configuration.

NODE_ENV=development

# CI / Docker build only
# SKIP_ENV_VALIDATION=1
